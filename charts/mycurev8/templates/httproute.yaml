{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "mycurev8.fullname" . }}
  namespace: {{ include "mycurev8.namespace" . }}
  labels:
    {{- include "mycurev8.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "mycurev8.gateway.name" . }}
      namespace: {{ include "mycurev8.gateway.namespace" . }}
      sectionName: https
  hostnames:
    - {{ include "mycurev8.gateway.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "mycurev8.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}

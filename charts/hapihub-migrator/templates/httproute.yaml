{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "hapihub-migrator.fullname" . }}
  namespace: {{ include "hapihub-migrator.namespace" . }}
  labels:
    {{- include "hapihub-migrator.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "hapihub-migrator.gateway.name" . }}
      namespace: {{ include "hapihub-migrator.gateway.namespace" . }}
      sectionName: {{ .Values.gateway.listener | default "https" }}
  hostnames:
    - {{ include "hapihub-migrator.gateway.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "hapihub-migrator.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
